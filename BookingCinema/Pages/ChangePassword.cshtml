@page
@model BookingCinema.Pages.ChangePasswordModel

<!DOCTYPE html>
<html>
<head>
    <title>Change Password Form</title>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/form.css" asp-append-version="true" />
</head>
<body>
 
    <div id="notification-container">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert alert-success" role="alert">
                @Html.Raw(@Model.Message)
            </div>
        }
    </div>
    <div class="center">
        <div class="main">
            <div class="login">
                @if (Model.ModelState.ContainsKey("LoginError"))
                {
                    <div class="alert alert-danger" role="alert">
                        @Html.Raw(Model.ModelState["LoginError"].Errors[0].ErrorMessage)
                    </div>
                }
               <div class="px-5">
                    <form method="post">
                        <label aria-hidden="true">Change Password</label>
                        <div class="form-group">
                            <input asp-for="NewPassword" placeholder="Please enter your new password" class="form-control input2" />
                            <span asp-validation-for="NewPassword" class="text-dark"></span>
                        </div>
                        <div class="form-group">
                            <input asp-for="ConfirmPassword" type="password" placeholder="Please enter your confirm password" class="form-control input2" />
                            <span asp-validation-for="ConfirmPassword" class="text-dark"></span>
                        </div>
                        <button class="button2" type="submit">Save</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
